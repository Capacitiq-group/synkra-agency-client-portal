# See ARCHITECTURE.md §7. This instance is dedicated to the Agency
# platform - NOT the same PocketBase instance as synkra-client-hub
# (Flow/Chat) or synkra--web-main's own instance.
VITE_POCKETBASE_URL=
POCKETBASE_URL=
POCKETBASE_ADMIN_EMAIL=
POCKETBASE_ADMIN_PASSWORD=

# Same Paystack merchant account as synkra-client-hub.
PAYSTACK_SECRET_KEY=

# This app's own public URL, for Paystack checkout callbacks.
APP_URL=
